{
  "name": "quenchz-interop-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Proves the Python server's audience and budget claims from outside the Python tree.",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "tsc && node scripts/run-proofs.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
